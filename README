You should have mongod running in your localhost. Instructions to install it in
mac os x can be found here:

http://shiftcommathree.com/articles/how-to-install-mongodb-on-os-x

mkvirtualenv --no-site-packages django-tastypie-nonrel

pip install -r requirements.txt

git subomdule init
git submodule update
